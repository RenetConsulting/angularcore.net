import { GoogleProblemComponent } from './google-problem.component';

describe('GoogleProblemComponent', () => {

    let component: GoogleProblemComponent;

    beforeEach(() => {
        component = new GoogleProblemComponent();
    });

    it('should create', () => {
        expect(component).toBeTruthy();
    });
});
