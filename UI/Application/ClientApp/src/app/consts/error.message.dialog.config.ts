import { MatDialogConfig } from "@angular/material/dialog";

export const ERROR_MESSAGE_DIALOG_CONFIG: MatDialogConfig = {
    maxWidth: 600,
    minWidth: 300,
    position: { bottom: '15px' },
    panelClass: 'error-message-dialog'
}