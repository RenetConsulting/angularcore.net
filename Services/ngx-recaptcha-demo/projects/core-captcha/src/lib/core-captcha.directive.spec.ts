import { CoreCaptchaDirective } from './core-captcha.directive';

describe('CoreCaptchaDirective', () => {
  it('should create an instance', () => {
    const directive = new CoreCaptchaDirective();
    expect(directive).toBeTruthy();
  });
});
