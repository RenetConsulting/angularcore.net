import { NgxRecaptchaDirective } from './ngx-recaptcha.directive';

describe('NgxRecaptchaDirective', () => {
    it('should create an instance', () => {
        const directive = new NgxRecaptchaDirective();
        expect(directive).toBeTruthy();
    });
});
