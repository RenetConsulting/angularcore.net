import { enterLeaveAnimation } from './animations';

describe('Animations', () => {

    beforeEach(() => {
    });

    it('should create', () => {
        expect(enterLeaveAnimation).toBeTruthy();
    });
});
